<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yogaappadmin"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.YogaAppAdmin"
        tools:targetApi="31">

        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Thêm các activity khác vào đây -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />  <!-- Thêm exported="true" cho MainActivity -->

        <activity android:name=".AddCourseActivity" />
        <activity android:name=".ViewCoursesActivity" />
        <activity android:name=".SearchClassesActivity" />
        <activity android:name=".EditCourseActivity" />
        <activity android:name=".AddClassInstanceActivity" />
        <activity android:name=".EditClassInstanceActivity" />
        <activity android:name=".ViewClassInstancesActivity" />
        <activity android:name=".RegisterActivity" />
    </application>

</manifest>